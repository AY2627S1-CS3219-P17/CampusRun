# AI Assistance Disclosure:
# Tool: Claude Code (model: Claude Opus 5.5), date: 2026-09-27
# Scope: AI-generated example of the frontend's local-development settings.
# Author review: <to be completed by author>

# Only used by the Vite dev server (`npm run dev` / `npm run preview`). Copy to `.env`.
# The app calls relative /api/... URLs; the dev server forwards them here.
# http://localhost:8080 is the gateway from `docker compose up` at the repo root.
VITE_API_PROXY_TARGET=http://localhost:8080
